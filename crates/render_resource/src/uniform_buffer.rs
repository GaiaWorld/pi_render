use std::sync::Arc;

use pi_atom::Atom;
use render_core::rhi::{
    device::RenderDevice,
    // dyn_uniform_buffer::{DynUniformBuffer, Bind, BindOffset, AsBind},
    buffer::Buffer,
    RenderQueue
};
